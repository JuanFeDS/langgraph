[project]
name = "langgraph-platzi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1.6",
    "langchain>=1.0.0a10",
    "langchain-openai>=1.0.0a3",
    "langgraph>=1.0.0a4",
    "python-dotenv>=1.1.1",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
    "langgraph-api>=0.4.31",
    "langgraph-cli[inmen]>=0.4.2",
]


[tool.setuptools.packages.find]
where = ["src"]
include = ["*"]
